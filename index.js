document.querySelector("main#main").remove()

const newHeader = document.createElement('h1')

newHeader.id = 'victory'

newHeader.innerHTML = "SAJIYA-MILI is the champion"



// The tests that say point to a node usually means that it wants you to create that node.
// has a 'newHeader' variable that points to an <h1> node

// so we want to use createElement here

    

